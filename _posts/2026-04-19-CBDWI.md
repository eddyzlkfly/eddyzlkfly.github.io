---
title: "CyberDefenders - Web Investigation"
date: 2026-04-19
categories: [Network Forensic, DFIR, CyberDefenders]
tags: [traffic analysis, wireshark]
description: Analysis of a web-based attack using PCAP traffic, focusing on SQL injection, credential compromise, and attacker activity through HTTP inspection.
image: /assets/img/CBDlogo.png
---
## Scenario

You are a cybersecurity analyst working in the Security Operations Center (SOC) of BookWorld, an expansive online bookstore renowned for its vast selection of literature. BookWorld prides itself on providing a seamless and secure shopping experience for book enthusiasts around the globe. Recently, you've been tasked with reinforcing the company's cybersecurity posture, monitoring network traffic, and ensuring that the digital environment remains safe from threats.

Late one evening, an automated alert is triggered by an unusual spike in database queries and server resource usage, indicating potential malicious activity. This anomaly raises concerns about the integrity of BookWorld's customer data and internal systems, prompting an immediate and thorough investigation.

As the lead analyst in this case, you are required to analyze the network traffic to uncover the nature of the suspicious activity. Your objectives include identifying the attack vector, assessing the scope of any potential data breach, and determining if the attacker gained further access to BookWorld's internal systems.

## Question 1

By knowing the attacker's IP, we can analyze all logs and actions related to that IP and determine the extent of the attack, the duration of the attack, and the techniques used. Can you provide the attacker's IP?

### Solutions

To begin the investigation, the provided `.pcap` file was opened using Wireshark.

The first step was to identify which network communication was the most significant. To achieve this, the **Conversations** feature in Wireshark was used.

### 🔍 Why use Conversations?

The **Conversations** tab provides a summary of communication between different IP addresses, including:

- Total packets exchanged
- Total bytes transferred
- Direction of traffic (A → B and B → A)

This helps quickly identify:

- The most active connections
- Potential suspicious or abnormal traffic
- High data transfers that may indicate exfiltration or malicious activity

Instead of manually inspecting thousands of packets, the **Conversations** view allows us to prioritize analysis by focusing on the most relevant traffic.

### 📊 Analysis

After navigating to **Conversations, t**he conversations were sorted based on the number of packets and bytes.

The following communication stood out:

![/assets/img/imgCBDWI.png](/assets/img/imgCBDWI.png)

This was significantly higher compared to other conversations, indicating that it is likely the primary communication of interest.

## Question 2

If the geographical origin of an IP address is known to be from a region that has no business or expected traffic with our network, this can be an indicator of a targeted attack. Can you determine the origin city of the attacker?

### Solutions

To identify the origin of the IP, an online IP geolocation tool was used:

```
https://iplocation.io
```

This tool aggregates data from multiple GeoIP databases and provides details such as:

- Country
- State/Region
- City
- ISP (Internet Service Provider)

### 📊 Result

![/assets/img/imgCBDWI(1).png](/assets/img/imgCBDWI(1).png)

Based on the lookup:

- **IP Address:** 111.224.250.131
- **Country:** China
- **State:** Hebei
- **City:** Shijiazhuang
- **ISP:** ChinaNet Hebei Province Network

## Question 3

Identifying the exploited script allows security teams to understand exactly which vulnerability was used in the attack. This knowledge is critical for finding the appropriate patch or workaround to close the security gap and prevent future exploitation. Can you provide the vulnerable PHP script name?

## Solutions

To focus specifically on web-related traffic, a display filter was applied in Wireshark:

```php
tcp.port == 80
```

![/assets/img/imgCBDWI(2).png](/assets/img/imgCBDWI(2).png)

### 📊 Analysis

By reviewing the filtered traffic, multiple HTTP requests were observed.

One notable request was:

```php
GET /search.php?s=arch+Bridge HTTP/1.1
```

The analysis of HTTP traffic revealed that the endpoint:

```php
search.php
```

was accessed during the communication.

### Question 4

Establishing the timeline of an attack, starting from the initial exploitation attempt, what is the complete request URI of the first SQLi attempt by the attacker?

### Solutions

To narrow down relevant web requests, the following Wireshark filter was applied:

```php
http contains ".php"
```

### Results

![/assets/img/imgCBDWI(3).png](/assets/img/imgCBDWI(3).png)

This filter helps isolate HTTP requests targeting dynamic PHP endpoints, which are commonly used in web applications and are frequent targets for injection attacks.

### 📊 Analysis

While reviewing the filtered traffic, multiple requests to `search.php` were observed.

One request stood out due to suspicious input patterns:

```php
GET /search.php?search=book%20and%201=1;%20--%20- HTTP/1.1
```

This request contains encoded characters (`%20`) and SQL-like syntax, indicating a possible injection attempt.

![/assets/img/imgCBDWI(4).png](/assets/img/imgCBDWI(4).png)

To better understand the request, the URI was decoded using an online URL decoder (meyerweb.com).

### Decoded result:

![/assets/img/imgCBDWI(5).png](/assets/img/imgCBDWI(5).png)

## Question 5

Can you provide the complete request URI that was used to read the web server's available databases?

### Solutions

The following filter was applied in Wireshark:

```php
http contains "schema"
```

### Results

![/assets/img/imgCBDWI(6).png](/assets/img/imgCBDWI(6).png)

This filter is useful because:

- The term **"schema"** is commonly associated with database structure
- Attackers often query `INFORMATION_SCHEMA` to enumerate databases, tables, and columns
- It helps quickly identify SQL injection attempts related to database discovery

After applying the filter, a suspicious request was identified containing encoded SQL keywords such as:

- `UNION SELECT`
- `INFORMATION_SCHEMA`

The captured request URI:

```php
/search.php?search=book%27%20UNION%20ALL%20SELECT%20NULL%2CCONCAT%280x7178766271%2CJSON_ARRAYAGG%28schema_name%29%2C0x71786a6d29%20FROM%20INFORMATION_SCHEMA.SCHEMATA--%20-
```

The URI was decoded using an online URL decoder.

## Question 6

Assessing the impact of the breach and data access is crucial, including the potential harm to the organization's reputation. What's the table name containing the website users data?

### Solutions

The HTTP traffic was reviewed again using the following filter:

```php
tcp.port == 80
```

### Results

![/assets/img/imgCBDWI(7).png](/assets/img/imgCBDWI(7).png)

This filter ensures that all web-related communications are visible, allowing deeper inspection of responses returned by the server.

During the inspection of HTTP responses, one packet revealed sensitive data returned from the server.

The response contained structured data resembling database output:

```php
<p>qxxvgb["admin","books","customers"]jqvpjq</p> ``` id="respdata"
```

## Question 7

The website directories hidden from the public could serve as an unauthorized access point or contain sensitive functionalities not intended for public access. Can you provide the name of the directory discovered by the attacker?

### Solutions

The following filter was applied in Wireshark:

```php
http.request.method == POST
```

### Results

![/assets/img/imgCBDWI(8).png](/assets/img/imgCBDWI(8).png)

This filter helps identify form submissions, which are commonly used for:

- Login pages
- Admin panels
- Sensitive functionalities

After applying the filter, several HTTP POST requests were observed targeting the following endpoint:

```php
/admin/login.php
```

## Question 8

Knowing which credentials were used allows us to determine the extent of account compromise. What are the credentials used by the attacker for logging in?

### Solutions

The following filter was applied in Wireshark:

```php
ip.src == 111.224.250.131 and http.request.method == POST
```

### Results

![/assets/img/imgCBDWI(9).png](/assets/img/imgCBDWI(9).png)

This filter isolates login attempts made by the attacker, specifically focusing on form submissions.

### 📊 Analysis

Multiple POST requests to the login endpoint were observed. 

The earlier requests showed repeated login attempts, indicating possible brute-force or credential guessing.

However, one request stood out as successful based on the server response (`HTTP/1.1 200 OK`) and returned content.

From this request, the following credentials were extracted:

```php
username=admin
password=admin123!
```

## Question 9

We need to determine if the attacker gained further access or control of our web server. What's the name of the malicious script uploaded by the attacker?

### Solutions

The same filter was reused to focus on attacker activity:

```php
ip.src == 111.224.250.131 and http.request.method == POST
```

### Results

![/assets/img/imgCBDWI(10).png](/assets/img/imgCBDWI(10).png)

This helps isolate actions performed by the attacker, including potential file uploads.

### 📊 Analysis

Among the POST requests, one request stood out:

```php
POST /admin/index.php
```

This request contained a `multipart/form-data` payload, which is typically used for file uploads.

After following the HTTP stream, the uploaded file details were revealed:

![/assets/img/imgCBDWI(11).png](/assets/img/imgCBDWI(11).png)